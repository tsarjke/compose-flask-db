# Counter app with postgres and docker-compose

```
docker-compose build
docker-compose up
```
There may be problems due to running the code before creating the database
Just run again:
```
docker-compose up
```